# SDD OpenSpec Config — solana-voting-dapp
# Auto-generated by sdd-init on 2026-07-13

project: solana-voting-dapp
repo_root: /home/Billy/solana-voting-dapp/voting
current_branch: feat/unified-frontend
persistence_mode: hybrid

context: |
  Solana voting dApp with 3 evolutionary levels in separate branches (no merge).
  V2 token-gated (Escrow) is the current program on feat/unified-frontend.
  V3 merkle-tree lives in voting.worktrees/agents-sdd-nivel-3.
  Frontend is Vite+React+TS with unified UI for V1/V2/V3.

stack:
  rust: "1.96.0"
  anchor: "1.1.2"
  testing: "LiteSVM 0.10.0"
  frontend: "Vite 6 + React 18 + TypeScript 5.7"
  bundler: "pnpm"
  anchor_spl: "1.1.2"

testing:
  strict_tdd: true
  program:
    runner: cargo
    command: "NO_DNA=1 cargo test"
    framework: LiteSVM
    layers:
      unit: { available: true, tool: LiteSVM }
      integration: { available: false, tool: "-" }
      e2e: { available: false, tool: "-" }
    coverage: { available: false, command: "-" }
  frontend:
    runner: vitest
    command: "pnpm test"
    framework: vitest 2.1.0
    layers:
      unit: { available: true, tool: vitest + testing-library }
      integration: { available: false, tool: "-" }
      e2e: { available: false, tool: "-" }
    coverage: { available: true, command: "vitest run --coverage" }
  quality:
    linter: { available: true, command: "cargo fmt --check / prettier --check" }
    type_checker: { available: true, command: "tsc" }
    formatter: { available: true, command: "cargo fmt / prettier" }

conventions:
  - "NO anchor test (mocha) — use NO_DNA=1 cargo test with LiteSVM"
  - "NO merge between branches — each level is independent"
  - "NO_DNA=1 prefix for all anchor commands (build, test, deploy)"
  - "NO SDD artifacts pushed to GitHub"
  - "Self-signing CPIs via PDAs for escrow operations"
  - "InitSpace required for all account structs"
  - "Fixed seeds per version — do not mix"
  - "pnpm for frontend, yarn not used"

phase_rules:
  propose: { max_pages: 2, required_sections: [intent, scope, approach] }
  spec: { required_sections: [requirements, scenarios] }
  design: { required_sections: [architecture, data_flow] }
  tasks: { max_minutes_per_task: 30 }
  verify: { required: [test_run, spec_match] }
